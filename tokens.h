namespace tokens {
    enum token_t {
        TOK_EOF,
        IF,
        WHILE,
        PRINT,
        READ,
        IDENTIFIER,
        INT,
        ASSIGN,
        PLUS,
        MINUS,
        MULT,
        DIV,
        SEMICOLON,
    };
}